{
  "name": "thewinequalityapp",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "deploy": "git add . && git commit -m 'build' && git push origin master",
    "build": "node build.js"
  },
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "marked": "^0.3.6"
  }
}
