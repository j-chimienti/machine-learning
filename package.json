{
  "name": "machine-learning",
  "author": "joe chimienti",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "deploy": "git add . && git commit -m 'build' && git push origin master",
    "build": "node build.js"
  },
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "marked": "^0.3.6"
  }
}
